name: Release

on:
  push:
    branches:
      - master
    tags:
      - 'v*.*.*'

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Release ${{ matrix.target }}
    runs-on: ubuntu-latest
    strategy:
      matrix:
        target: [ x86_64-unknown-linux-gnu, x86_64-pc-windows-gnu, aarch64-apple-darwin, x86_64-apple-darwin ]

    steps:
    - uses: actions/checkout@v2

    - name: Install rust
      uses: actions-rs/toolchain@v1
      with:
        toolchain: stable
        profile: minimal
        override: true
        target: ${{ matrix.target }}

    - name: Build target
      uses: actions-rs/cargo@v1
      with:
        use-cross: true
        command: build
        args: --release --target ${{ matrix.target }}

    - name: Package
      shell: bash
      run: |
        #strip target/${{ matrix.target }}/release/testmest
        cd target/${{ matrix.target }}/release
        tar czvf ../../../testmest-${{ matrix.target }}.tar.gz poe_query
        cd -

    - name: Publish
      uses: softprops/action-gh-release@v1
      with:
        files: 'poe_query*'
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
